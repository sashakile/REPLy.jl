# Agent config projections — defines how configs are synced to tool-specific locations
# Strategies: symlink, inline, reference
# Built-in target: claude-code (no strategy/sources needed)
projections:
  - target: claude-code

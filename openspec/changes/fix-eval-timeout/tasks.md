## 1. Red — specify failing behavioral and race tests
- [x] 1.1 Add barrier-controlled tests that hold an eval live across its deadline, issue interrupt before the deadline, and separately release it before/after the deadline; assert observed pre-deadline termination yields interrupted completion, a live-at-deadline task becomes zombie despite cancellation, and the response stream records exactly one terminal response.
- [x] 1.2 Add accounting tests that each live eval retains exactly one EvalGate permit, active-task registration, and session/resource charge until actual termination, with exactly-once completion cleanup.
- [x] 1.3 Add latch-synchronized named-session tests that hold `eval_lock` and task completion blocked while normal operations, `stdin`, and `close` return without acquiring or waiting on `eval_lock`, EvalGate, or task completion; benchmark responses at 100 ms p99 on reference hardware and verify best-effort idempotent `interrupt`.
- [x] 1.4 Add barrier-controlled timeout/close and alias-reuse races proving that close with a live task follows `EVAL_RUNNING`/`QUARANTINED` → `DETACHED` → `DESTROYED`, while close after a quarantined zombie has terminated performs normal teardown and follows `QUARANTINED` → `DESTROYED` directly; assert immediate discovery removal, hidden `max_sessions` accounting only while work remains live, bounded no-wait behavior in both branches, replacement lookup through the interrupt request's `session` field, loss of old-eval alias recovery, an idempotent no-op when `interrupt-id` filters for the old detached eval, and object-identity-keyed exactly-once cleanup that cannot affect the replacement.
- [x] 1.5 Add barrier-controlled ephemeral-zombie and capacity tests proving retained accounting, rejection of new and awakened queued acquisitions without acquiring or waiting on EvalGate, `eval_lock`, or task completion, 100 ms p99 responses on reference hardware, and eventual cleanup after explicit task release.

## 2. Green — implement the approved lifecycle
- [x] 2.1 Implement interrupt as an idempotent cancellation request and add the atomic deadline/completion transition so a task live at deadline becomes zombie and emits exactly one timeout terminal response without releasing live-work accounting.
- [x] 2.2 Centralize task completion so EvalGate release, active-task deregistration, hidden-session/ephemeral teardown, and resource release each occur exactly once.
- [x] 2.3 Enforce permanent named-session quarantine and route rejected operations, idempotent interrupt, and close without acquiring or waiting on `eval_lock`, EvalGate, or task completion, meeting 100 ms p99 on reference hardware.
- [x] 2.4 Implement liveness-based close: atomically remove discovery without acquiring or waiting on `eval_lock`, EvalGate, or task completion; for a live task, enter `DETACHED`, retain hidden accounting, and defer object-identity-keyed teardown to `DESTROYED`; for a quarantined object whose zombie already terminated, perform normal exactly-once teardown and transition directly to `DESTROYED`; permit safe alias replacement in both branches.
- [x] 2.5 Wake and reject queued EvalGate acquisitions when zombie-held permits saturate capacity.

## 3. Refactor and verify
- [x] 3.1 Consolidate lifecycle/accounting invariants without changing behavior.
- [x] 3.2 Run focused race tests, the full test suite, `openspec validate fix-eval-timeout --strict`, and `ah check --changes fix-eval-timeout`.
- [x] 3.3 Document that only process termination hard-reclaims non-cooperative in-process evals and that heavy process-isolated sessions are the hard-bound option.
